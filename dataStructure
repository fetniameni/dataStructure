function sumDistinctElements(set1,set2){
    var sum=0
    for(var i=0; i < set1.length; i++){
        for(var j=0; j < set2.length; j++){
            if(set1[i]===set2[j]){
                sum+= set1[i] + set2[j]
            }
        }
    }
    return sum
}